#!/bin/sh

echo Usage:
echo -------------------------------------------------------------
echo install: spkg install item-name (you will be asked for repo that you want install from)
echo -------------------------------------------------------------
echo sync script from url: spkg sync item-url wanted-item-name (you will be asked for name that you want save script as if you wont enter name )
echo -------------------------------------------------------------
echo update script: spkg update item-name
echo -------------------------------------------------------------
echo remove script: spkg remove item-name
echo -------------------------------------------------------------
echo list scripts: spkg list (if you install items with sudo then you need also use sudo here)
echo -------------------------------------------------------------
echo add repo: spkg repo add repo-url
echo -------------------------------------------------------------
echo remove repo: spkg repo remove repo-name
echo -------------------------------------------------------------
echo get list of core repos that exist: spkg repo getlist
echo -------------------------------------------------------------
echo get configuration of item or destination: spkg catconf item
echo -------------------------------------------------------------
echo more info: https://github.com/tuxedlinux/scriptpkg
