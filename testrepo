htop="pwd; git clone https://github.com/htop-dev/htop htopgit; cd htopgit; ./autogen.sh && ./configure && make"
${!1}
cp $1 $(spkg catconf destination)
cd ~/.config/spkg/packages
echo "spkg 0" > $1
echo "${!1}" >> $1
